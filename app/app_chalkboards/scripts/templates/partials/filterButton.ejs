<select name="select-choice" class="choose-filter"  id="select-choice">
	<option>Filter</option>
    <% _(filters).each(function (filter) { %>
        <option class="tiles_filter"><%= filter.domain %></option>
    <% }) %>
</select>