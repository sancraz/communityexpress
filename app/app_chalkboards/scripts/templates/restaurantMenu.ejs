<% buttons.forEach(function (button) { %>
	<div class="sasl_menu_button sprite_navbar <%= button.key %> <%= button.className %>">
		<img src="">
		<span><%= button.displayText %></span>
	</div>
<% }) %>
<a class="close_button outside" href="#">close</a>
